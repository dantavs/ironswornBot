import { MoveResultDescriptorProps } from "../interfaces/MoveResultDescriptor";

export class MoveDescriptor{
    props: MoveResultDescriptorProps
    constructor (props: MoveResultDescriptorProps){
        this.props = props
    }
}